{
  "name": "tsoa-example-models",
  "version": "1.0.0",
  "description": "models for example typescript SoA app",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "build": "npm run clean && tsc",
    "clean": "rimraf dist",
    "lint": "tslint --project tslint.json",
    "prepublish": "npm run build",
    "test": "npm run lint && mocha tests/**/*.ts --require ts-node/register"
  },
  "repository": {
    "type": "git",
    "url": "git+https://github.com/stevenalexander/node-tsoa-example-models.git"
  },
  "author": "Steven Alexander",
  "license": "MIT",
  "bugs": {
    "url": "https://github.com/stevenalexander/node-tsoa-example-models/issues"
  },
  "homepage": "https://github.com/stevenalexander/node-tsoa-example-models#readme",
  "devDependencies": {
    "@types/mocha": "^2.2.48",
    "@types/node": "^9.4.7",
    "mocha": "^5.0.4",
    "rimraf": "^2.6.2",
    "ts-node": "^5.0.1",
    "tslint": "^5.9.1",
    "typescript": "^2.7.2"
  }
}
